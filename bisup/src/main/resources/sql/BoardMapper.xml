<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="board"><!--로그인에 필요한 맵퍼 파일 -->
	<select id="selectBoardList" parameterType="map" resultType="map">
	</select>

	<select id="selectboardContents" parameterType="map" resultType="map">
	</select>
	
	<select id="allCnt" parameterType="map" resultType="map">
	</select>

	<insert id="insertBoard" parameterType="hashmap">
	</insert>

	<update id="updateCnt" parameterType="hashmap">
	</update>


	<update id="deleteBoard" parameterType="hashmap">
	</update>
	
	
	

</mapper>